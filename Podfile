# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'CinemaAssistant' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_modular_headers!

  # Pods for CinemaAssistant
  pod "RealmSwift"
  pod "SwiftyJSON"
  pod "Alamofire"
  pod "Firebase"
  pod "Firebase/Auth"
  pod "Firebase/Database"
  pod "SwiftSoup"
end
